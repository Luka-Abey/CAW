{
  "name": "caw",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@types/react-router": "^5.1.11",
    "concurrently": "^5.3.0",
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "start": "npm start --prefix client",
    "server": "node index.js --prefix server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"nodemon run server\" \"npm run client\"",
    "heroku-prebuild": "cd client && npm i && npm start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
